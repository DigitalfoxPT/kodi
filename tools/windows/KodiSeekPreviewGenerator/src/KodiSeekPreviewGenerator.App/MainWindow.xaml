<Window
    x:Class="KodiSeekPreviewGenerator.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Kodi Seek Preview Generator">

    <Grid x:Name="RootLayout"
          Padding="28"
          RowSpacing="18"
          Loaded="RootLayout_Loaded"
          Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="6">
            <TextBlock Text="Kodi Seek Preview Generator"
                       Style="{ThemeResource TitleTextBlockStyle}" />
            <TextBlock Text="Cria um ficheiro .bif por vídeo, com uma imagem de 10 em 10 segundos. Analisa também todas as subpastas."
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       TextWrapping="Wrap" />
        </StackPanel>

        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="FolderPathBox"
                     Header="Pasta principal dos vídeos"
                     PlaceholderText="Escolha a pasta Shows ou outra pasta principal"
                     IsReadOnly="True" />
            <Button x:Name="BrowseButton"
                    Grid.Column="1"
                    Margin="0,24,0,0"
                    Content="Escolher pasta"
                    Click="BrowseButton_Click" />
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="12">
            <Button x:Name="GenerateButton"
                    Content="Analisar e gerar previews"
                    Style="{ThemeResource AccentButtonStyle}"
                    Click="GenerateButton_Click" />
            <Button x:Name="CancelButton"
                    Content="Cancelar"
                    IsEnabled="False"
                    Click="CancelButton_Click" />
        </StackPanel>

        <StackPanel Grid.Row="3" Spacing="8">
            <ProgressBar x:Name="GenerationProgressBar"
                         Minimum="0"
                         Maximum="1"
                         Value="0" />
            <InfoBar x:Name="StatusInfoBar"
                     IsOpen="True"
                     IsClosable="False"
                     Severity="Informational"
                     Title="Pronto"
                     Message="Selecione a pasta e inicie a análise." />
        </StackPanel>

        <Border Grid.Row="4"
                Padding="12"
                CornerRadius="8"
                BorderThickness="1"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <ListView x:Name="LogList"
                      ItemsSource="{x:Bind LogMessages}"
                      SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}"
                                   FontFamily="Consolas"
                                   FontSize="12"
                                   TextWrapping="Wrap" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Window>
